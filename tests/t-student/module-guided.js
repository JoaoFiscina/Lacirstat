import { createDatasusWizard } from '../../assets/js/datasus-wizard.js';
import {
  deriveIndependentTTest,
  derivePairedTTest,
  findBestNormalizedPair,
  getCategoryOptions,
  getMetricOptions,
  getPrimaryMetricKey,
  getTimeOptions
} from '../../assets/js/datasus-normalizer.js';
import {
  parseDataset,
  safeWelch,
  safePaired,
  renderAnalysisError,
  buildResultMetricsHtml,
  buildResultChartsHtml,
  buildManualInterpretation
} from './module.js';

export async function renderTestModule(ctx) {
  void createDatasusWizard;
  void deriveIndependentTTest;
  void derivePairedTTest;
  void findBestNormalizedPair;
  void getCategoryOptions;
  void getMetricOptions;
  void getPrimaryMetricKey;
  void getTimeOptions;
  void parseDataset;
  void safeWelch;
  void safePaired;
  void renderAnalysisError;
  void buildResultMetricsHtml;
  void buildResultChartsHtml;
  void buildManualInterpretation;
  void ctx;
}
